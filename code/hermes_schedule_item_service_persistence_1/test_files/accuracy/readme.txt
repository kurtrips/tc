1.
HermesScheduleItemPersistenceHelper is not tested explicitly, because this design is not clear enough, then all developers dev it differently, but i test it when testing HermesScheduleItemPersistenceProvider. We also can ensure whether this component can work in this way.
2.
change the connect string.